@model ReplayFXSchedule.Web.Models.EventType

@{
    ViewBag.Title = "Edit Event Type";
}
<div class="card">
    <div class="card-header">
        <h4 class="card-title">Create Event Type</h4>
    </div>
    <div class="card-body">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            @Html.Partial("_form")
        }
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
